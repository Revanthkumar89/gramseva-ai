const router=require("express").Router(),multer=require("multer"),path=require("path"),fs=require("fs");const {authenticate}=require("../middleware/auth");const dir=path.join(process.cwd(),"uploads");fs.mkdirSync(dir,{recursive:true});const upload=multer({storage:multer.diskStorage({destination:dir,filename:(_,f,cb)=>cb(Date.now()+"-"+f.originalname.replace(/[^a-zA-Z0-9._-]/g,"_"))}),limits:{fileSize:5*1024*1024}});router.post("/complaint-photo",authenticate,upload.single("photo"),(req,res)=>{if(!req.file)return res.status(400).json({message:"Photo required"});res.json({imageUrl:`${req.protocol}://${req.get("host")}/uploads/${req.file.filename}`})});module.exports=router;