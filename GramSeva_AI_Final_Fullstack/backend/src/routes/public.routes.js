const router=require("express").Router();const prisma=require("../config/prisma");router.get("/issues",async(req,res,next)=>{try{const complaints=await prisma.complaint.findMany({orderBy:{createdAt:"desc"},take:100,select:{complaintId:true,category:true,description:true,village:true,location:true,priority:true,status:true,createdAt:true}});res.json({complaints})}catch(e){next(e)}});module.exports=router;